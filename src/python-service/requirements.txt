# Ledger Audit Service Dependencies
# Production dependencies only - minimal footprint

# Testing
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0

# Linting (dev)
ruff>=0.1.0,<1.0.0

# Type checking (dev)
mypy>=1.5.0,<2.0.0
